<svg
  viewBox="{% if include.viewBox %}{{ include.viewBox }}{% else %}0 0 16 16{% endif %}"
  preserveAspectRatio = "xMidYMid meet"
  class="svg svg-{{ include.id }}">
  {% if include.title %}
    <title id="title-{{ include.id }}">{{ include.title }}</title>
  {% endif %}
  <use xlink:href="#{{ include.id }}"></use>
</svg>
